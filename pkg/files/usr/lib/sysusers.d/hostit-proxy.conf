# hostit-proxy runs as this unprivileged system user; the internet-facing data
# plane holds no state beyond its route/cert cache. Created by the deb.
u hostit-proxy - "hostit proxy" - /usr/sbin/nologin
