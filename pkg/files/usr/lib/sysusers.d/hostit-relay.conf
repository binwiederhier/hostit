# The group SSH-relay stub accounts belong to on the control frontend. The
# sudoers grant and the sshd forwarding lockdown are both scoped to it. Separate
# from hostit-apps (the node's real app users) so a control-only frontend needs
# nothing the node package owns. Created by the deb (systemd-sysusers).
g hostit-relay -
