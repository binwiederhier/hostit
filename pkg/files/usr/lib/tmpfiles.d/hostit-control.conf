# control's sockets (control.sock and the cluster member socket) live here, owned
# by hostit-control. The shared parent is root-owned. See hostit-control.service.
d /run/hostit         0755 root           root           -
d /run/hostit/control 0755 hostit-control hostit-control -
