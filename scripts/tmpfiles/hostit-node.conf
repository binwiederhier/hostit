# the root node owns node.sock plus the app-socket subdir bind-mounted into
# containers. Root-owned; the node does the machine work.
d /run/hostit          0755 root root -
d /run/hostit/node     0755 root root -
d /run/hostit/node/app 0755 root root -
