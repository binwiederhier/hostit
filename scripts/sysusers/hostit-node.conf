# Reserved for symmetry and future use; the node runs as root today (it does the
# machine work -- unix users, podman, btrfs, nftables -- that needs it).
u hostit-node - "hostit node" - /usr/sbin/nologin
