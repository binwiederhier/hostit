# Copy to inventory.ini and set your host. hostit runs on a single host per
# deployment (the whole control plane is one binary).

[hostit]
my-hostit-server ansible_host=203.0.113.10 ansible_user=root
