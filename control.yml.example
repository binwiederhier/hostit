# hostit control-plane config -- copy to /etc/hostit/control/control.yml and edit.
# Only base-domain and admin-token are required; everything else has sane defaults.

# Apps are served at <app>.<base-domain>. Requires a wildcard DNS record:
#   *.apps.example.com. A <ip-of-this-host>
#   apps.example.com.   A <ip-of-this-host>   (for SSH and the bare domain)
base-domain: apps.example.com

# Bearer token for the admin REST API. Generate with: openssl rand -hex 24
admin-token: CHANGE-ME

# TLS: "letsencrypt" (default, on-demand per-subdomain certs) or "off" (plain
# HTTP on listen-http; for development or behind another TLS-terminating proxy).
# tls: letsencrypt
# letsencrypt-email: you@example.com

# Wildcard TLS via DNS-01, instead of on-demand per-subdomain certs. With a DNS
# provider configured hostit obtains ONE *.<base-domain> certificate, so a brand
# new app serves TLS immediately. Only "route53" is supported. The AWS values are
# optional and fall back to the usual AWS env vars / instance credentials.
# dns-provider: route53
# aws-region: us-east-1
# aws-access-key-id: ...
# aws-secret-key: ...
# aws-hosted-zone-id: ...

# Web login (Google OAuth). Leave empty to run token-only with no web app. Create
# credentials at https://console.cloud.google.com/apis/credentials with the
# redirect URI https://<base-domain>/auth/callback.
# google-client-id: ...
# google-client-secret: ...

# Emails that become active admins on their first Google login (skip approval).
# admin-emails:
#   - you@example.com

# Built-in AI assistant (optional). The in-browser chat that can build and change
# an app. Leave everything unset to disable it (the chat UI hides itself).
#
# Metered Anthropic API (pay per token, uses your API credit):
# anthropic-api-key: sk-ant-...
# assistant-model: claude-sonnet-5           # default model for API turns
#
# Additionally offer Claude.ai (your Claude Pro/Max subscription) as a model in the
# chat, run per turn as `claude -p` in a locked-down podman sandbox (tools restricted
# to this app's own REST surface). Setting the token is all it takes; there is no
# backend switch. Get the token with: claude setup-token
# claude-code-oauth-token: ...
#
# Optional: the model menu shown in the chat (id + human label).
# assistant-models:
#   - {id: claude-sonnet-5, label: "Sonnet (balanced)"}
#   - {id: claude-haiku-4-5, label: "Haiku (fast, cheap)"}

# How the dashboard's app cards are previewed: "live" embeds each running app
# in a scaled-down iframe (default), "screenshot" shows a headless-chrome
# screenshot instead, "off" drops the card preview. The workspace's
# assistant/files preview panes are always live, regardless of this setting.
#
# In screenshot mode each running app is re-shot every 6 hours, plus ~1 minute
# after the assistant last changes it (max 5/hour per app), plus on demand from
# the card's refresh button. Shots run one at a time in a sandboxed podman
# container (image pulled on first use).
# app-preview: live
#
# In screenshot mode the shot container's network is confined: "strict" (the
# default) lets it reach only the target app's resolved IP and the public
# internet, blocking the host, the LAN/VPC and the cloud metadata endpoint;
# "off" applies no filter (only safe on a fully trusted network). This works
# whether the app resolves to a public or a private IP.
# app-preview-isolation: strict
#
# Extra destination CIDRs the shot container may additionally reach in strict
# mode, e.g. an internal asset/CDN host your apps load from.
# app-preview-allow-cidrs:
#   - 10.20.0.0/24

# Let the admin token mint a session cookie for an admin email without Google
# (for end-to-end testing and recovery). Off by default.
# breakglass: false

# Secret for signing session cookies. Generated and persisted under data-dir if
# left empty; set it only to share sessions across a rebuild.
# session-key: ...

# Listeners (defaults shown)
# listen-http: ":80"
# listen-api: "127.0.0.1:2900"   # optional extra plain-HTTP admin API listener
# socket-file: /run/hostit/hostit.sock

# Directories (defaults shown)
# data-dir: /var/lib/hostit      # registry DB + certs
# apps-dir: /var/lib/hostit/apps # app user home directories

# Hostnames (defaults shown)
# api-host: apps.example.com   # admin API + web app hostname (default: <base-domain>)
# ssh-host: apps.example.com   # SSH hostname reported to clients (default: <base-domain>)

# Per-app loopback port range (defaults shown)
# port-min: 10000
# port-max: 19999
